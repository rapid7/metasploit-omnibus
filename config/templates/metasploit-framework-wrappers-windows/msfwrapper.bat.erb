@echo off

setlocal enabledelayedexpansion

for /F %%i in ("%0") do set CMD=%%~ni
set SCRIPTDIR=<%= install_dir %>/bin
set SCRIPTDIR=C:/metasploit-framework/bin
set EMBEDDED=%SCRIPTDIR%/../embedded
set BIN=%EMBEDDED%/bin
set FRAMEWORK=%EMBEDDED%/framework

set GEM_HOME=
set GEM_PATH=
set GEM_ROOT=
set RUBY_ENGINE=
set RUBY_ROOT=

set PATH=%BIN%;%SCRIPTDIR%;%PATH%
%BIN%/ruby %FRAMEWORK%/%CMD% "%*"
endlocal
